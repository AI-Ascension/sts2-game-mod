# Native treasure evidence — 2026-09-06

## Confirmed Windows fixture result

The isolated Train Windows v0.107.1 campaign resumed visibly in the existing RDP session.
OpenAI Astra (`gpt-6-astra`) selected the unopened chest, selected its Gorget relic, and
selected Proceed. All three operations settled through native controls and host observations.
Proceed exposed a map with rest-site and shop choices. No OS input automation was used.

An initial candidate collected the relic visibly but kept its operation unknown because
the displayed model was not the inventory instance. The corrected candidate retained the
selected model identity and required a newly owned matching instance plus the selected
holder becoming unavailable. A fresh process then confirmed that completion check. A stale
generation rejection refreshed the legal action before dispatch; unknown operations were
never blindly retried.

The final Astra choice entered the shop, which lacked runtime controls. That operation
remained unknown and the whole harness exited 2. This proves bounded treasure continuation,
not a full campaign or successful whole harness invocation.

The fresh process also restored the earlier rest-site results: 79/87 HP and Bash+ in the
16-card deck. This adds bounded save-restoration evidence to the prior rest-site test.

## Artifact identity

The eleven-line prefix ending at the visible map has SHA-256
`5431d67681658c4b21aaa3e05f381414b214f192788988021ea98f95e3192093`.
The completed trajectory has SHA-256
`e58d0a3cf5ef94450822efcb6ad3f9741b84a3e4de2cff149d7f009c13f24f35`.
Private trajectories and fixture profiles remain outside Git.

| Installed file | SHA-256 |
| --- | --- |
| Managed addon | `391d15adb9d078e4c6b7707b3866847469e599307b9bf287975041d9eef998b2` |
| Native library | `5106c63cb8e6c88819de6c9a704248d2537ced59c73412fb8350fe32bb5cd7b9` |
| Addon manifest | `559e177f0b6e5d82fc44f6b086b1e728353b2f6e437f5e8fae98983d85659984` |

The addon was hash-verified before launch. Exact-host packaging passed with zero warnings
and errors. Strict policy passed with 244 sized files before this evidence document was added.
The managed gameplay probe passed; its host-independent checks do not exercise treasure UI.
Linux gameplay, multiplayer voting, special relic selection and full campaign completion
remain unverified. CI and exact-head merge checks remain separate requirements.
